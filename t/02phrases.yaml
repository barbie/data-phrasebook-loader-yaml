---
find_series:
    sql: select title,author from books where class = :series
find_author:
    sql: select title,author from books where author = :author
count_author:
    sql: >
        select count(1)
        from books
        where author = :author

# vim: et
